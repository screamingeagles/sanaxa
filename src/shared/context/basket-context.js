import { createContext } from "react";

export const BasketContext = createContext({
	//  isLoggedIn: false,
	// userId: null,
	// token: null,
	// login: () => {},
	// logout: () => {},
	setBasketContent: () => {},
	showBasketHandler: () => {},
});
